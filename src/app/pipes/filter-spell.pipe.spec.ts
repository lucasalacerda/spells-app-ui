import { FilterSpellPipe } from './filter-spell.pipe';

describe('FilterSpellPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSpellPipe();
    expect(pipe).toBeTruthy();
  });
});
